<DataGridPro
  rows={rows}
  columns={columns}
  getRowId={getRowId}
  groupingColumnMode="multiple"
  groupingColumnsPanel
  initialState={{
    groupingColumns: {
      model: ['company', 'director'],
    },
  }}
/>